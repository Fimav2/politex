import csv

with open("students.csv", encoding="utf8") as file:
    reader = csv.reader(file, delimiter=",")
    answer = list(reader)
    idi = input()
    for id, name, titleProject_id, clas, score in idi:
        if idi == id:
            fio = name.split()
            print("Проект №",titleProject_id,"делал:", fio[1][0] + "." + fio[0])
        flag = True
        break
if not flag:
    print("Ничего не найдено")
idi = input()
